/*1. Implemente uma função que receba como parâmetro um vetor de números inteiros (vet) de tamanho n e inverta a ordem dos elementos armazenados nesse vetor.*/
#include <stdio.h>
#include <stdlib.h>
float inverter(float a);
int main(void)
{
    // int n;

    // int vet[n];

    // scanf("%d",&n);

    // inverter(vet[n]);
    // printf("%d",vet[n]);
    printf("Nao consegui");
}